Example: Struct with Array Members (Student Marks System)
Use a struct that contains:

Normal variables
An array inside the struct

Problem

Create a program to:

Store student information
Each student has:
Name
ID
Array of marks (for 5 subjects)
Calculate:
Total marks
Average
Grade
